// import {useSelector} from 'react-redux'

const Home = () => {
  // const {auth} = useSelector((state) => ({...state}))
  return (
    <div className="container-fluid">
      <h1> Home Page</h1>
    </div>
  )
}

export default Home
